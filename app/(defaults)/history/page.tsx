import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: 'History',
};

const Page = () => {
    return <div>hostory page</div>;
};

export default Page;
